现在请你帮我统一整个项目的 UI 组件体系。

当前项目中混用了：

- Base UI
- Radix
- 自定义 Button
- shadcn/ui 风格组件

已经导致：

- Button render/asChild/nativeButton 冲突
- Link 嵌套 Button 报错
- 类型不兼容
- 组件行为不一致

例如：

Base UI 的 Button 不支持我现在使用的 render / asChild 写法。

所以现在请你：

# 全面统一为 shadcn/ui 风格体系

不要继续使用 Base UI。

不要混用不同 Button 实现。

不要保留 nativeButton/render 相关逻辑。

---

# 核心要求

请：

1. 删除 Base UI Button 相关实现
2. 删除 render prop 写法
3. 删除 nativeButton 相关逻辑
4. 所有按钮统一使用：
   - shadcn/ui Button
   - className
   - variant
   - size
5. 所有链接按钮统一使用：
   - Button asChild
   - 内部嵌套 Link

标准写法：

```tsx
<Button asChild variant="outline">
  <Link href="/projects">
    查看项目
  </Link>
</Button>
````

---

# 不允许再出现：

```tsx
render={<Link />}
```

或者：

```tsx
nativeButton
```

或者：

```tsx
<Button>
  <a>
```

这种错误结构。

---

# 请检查并修复以下文件中的 Button 使用：

* hero-section.tsx
* featured-projects.tsx
* about/page.tsx
* footer.tsx
* navbar.tsx
* 所有 components/ui/button.tsx 使用处

---

# Button 组件要求

请重新生成：

```bash
src/components/ui/button.tsx
```

要求：

* 完全兼容 shadcn/ui
* 使用 class-variance-authority
* 支持：

  * variant
  * size
  * asChild
* 使用 Radix Slot
* TypeScript 类型正确
* 不再使用 Base UI

---

# Link 使用规范

内部路由：

```tsx
<Link href="/about">
```

外部链接：

```tsx
<Link
  href="https://github.com/xxx"
  target="_blank"
  rel="noopener noreferrer"
>
```

---

# 修复目标

最终确保：

* 不再出现 nativeButton 报错
* 不再出现 Button render 报错
* 不再出现 Link/Button 嵌套警告
* 所有按钮交互一致
* 类型检查通过
* npm run dev 无报错
* npm run build 可通过

---

# 最后请：

1. 自动修复所有错误 Button 用法
2. 保持现有 UI 风格不变
3. 不要破坏动画与样式
4. 给关键修改加注释

```
```
